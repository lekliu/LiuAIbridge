# 🌉 LiuAIbridge 使用说明书

## 目录
1. [项目简介](#项目简介)
2. [功能特性](#功能特性)
3. [部署指南](#部署指南)
4. [本地开发调试](#本地开发调试)
5. [管理后台使用](#管理后台使用)
6. [API 调用指南](#api-调用指南)（含 [路由与协议约定](#路由与协议约定)）
7. [常见问题](#常见问题)

---

## 项目简介

**LiuAIbridge** 是一个轻量级、安全、带可视化 UI 的 AI API 边缘网关，部署在 Cloudflare Workers 上。专为解决国内开发者直连访问 Google Gemini、OpenAI、Anthropic 等主流 AI API 的网络问题而设计。

### 解决的问题
- ✅ 国内无需本地代理即可访问海外 AI API
- ✅ 统一管理多个平台的 API Key
- ✅ 可视化后台配置，操作简单
- ✅ 自带安全鉴权机制

---

## 功能特性

### 🚀 核心功能
1. **多平台支持**
   - Google Gemini
   - OpenAI (GPT 系列)
   - Anthropic (Claude 系列)

2. **智能代理转发**
   - Cloudflare 全球网络加速
   - 自动负载均衡（多 Key 轮询）
   - 请求统计与成功率追踪

3. **OpenAI 形态兼容（下游统一用 OpenAI Chat 协议）**
   - **Google**：`POST .../google/v1/chat/completions` → 网关将请求转为 **Gemini 原生** `generateContent` / `streamGenerateContent`，响应再转为 OpenAI JSON / SSE。
   - **Anthropic**：`POST .../anthropic/v1/chat/completions` → 网关转发至 **Anthropic 官方 OpenAI SDK 兼容端**（仍为 Claude 官方服务），请求/响应/流式由上游按 OpenAI 约定返回；能力边界以 [官方兼容说明](https://docs.anthropic.com/en/api/openai-sdk) 为准。
   - **固定前缀**：`/compat/openai/google/...`、`/compat/openai/anthropic/...` 与上述 `/google/...`、`/anthropic/...` **等价**，可同时使用。
   - **原生路径保留**：如 `/google/v1beta/...`、`/anthropic/v1/messages` 等，与 OpenAI 形态路径通过 **URL 路径区分**。
   - 当前跨厂商兼容以 **`/v1/chat/completions`**（含流式）为主；**`/v1/responses`** 未接入网关。

4. **安全加固**
   - 双 Token 权限体系（管理 Token / 桥接 Token）
   - API Key 加密存储于 Cloudflare KV
   - CORS 跨域支持

5. **可视化管理**
   - 内置 `/admin` 仪表盘
   - API Key 增删改查
   - 使用统计实时展示
   - 一键启用/停用 Key

---

## 部署指南

### 前置准备
1. 拥有 [Cloudflare](https://dash.cloudflare.com/) 账号
2. 安装 [Node.js](https://nodejs.org/) (推荐 LTS 版本)
3. 安装 Wrangler CLI：`npm install -g wrangler`

### 部署步骤

#### 步骤 1：克隆项目
```bash
git clone https://github.com/lekliu/LiuAIbridge.git
cd LiuAIbridge
npm install
```

#### 步骤 2：登录 Cloudflare
```bash
wrangler login
```
按照浏览器提示完成授权登录。

#### 步骤 3：创建 KV 命名空间
```bash
wrangler kv namespace create LIU_BRIDGE_KV
```
执行后会返回一个 ID，复制下来。

#### 步骤 4：配置 wrangler.toml
打开 `wrangler.toml` 文件，将第 31 行的 `id` 替换为上一步获得的 KV ID：
```toml
[[kv_namespaces]]
binding    = "LIU_BRIDGE_KV"
id         = "你的_KV_ID"  # ← 替换这里
preview_id = "你的预览_KV_ID"  # 可选，用于本地开发
```

#### 步骤 5：设置安全 Token
```bash
# 设置管理员 Token（登录后台用，建议复杂密码）
wrangler secret put ADMIN_TOKEN

# 设置桥接 Token（API 调用鉴权用）
wrangler secret put BRIDGE_TOKEN
```
执行每条命令后，在提示符后输入对应的 Token 值。

#### 步骤 6：部署到 Cloudflare
```bash
wrangler deploy
```
部署成功后，控制台会显示你的 Worker 域名，格式类似：
`https://liuaibridge.your-name.workers.dev`

🎉 恭喜！部署完成！

---

## 本地开发调试

### 1. 配置本地环境变量
在项目根目录创建 `.dev.vars` 文件：
```text
ADMIN_TOKEN=your_admin_password_123
BRIDGE_TOKEN=your_bridge_token_456
```

### 2. 启动本地开发服务器
```bash
npx wrangler dev
```
启动成功后，访问 `http://localhost:8788`

### 3. 初始化本地数据
1. 访问 `http://localhost:8788/admin`
2. 输入 `.dev.vars` 中设置的 `ADMIN_TOKEN`
3. 添加你的 API Key 进行测试

### 4. 注意事项
⚠️ **本地调试时**，你的电脑需要能够正常访问 Google/OpenAI API（可能需要开启全局代理）。部署到 Cloudflare 后则无需本地代理。

---

## 管理后台使用

### 访问管理后台
```
https://你的域名/admin
```
输入 `ADMIN_TOKEN` 登录。

### 功能说明

#### 🔑 API Key 管理
每个平台（Google / OpenAI / Anthropic）独立管理：

**添加新 Key：**
1. 点击「添加新 Key」按钮
2. 填写 Key 名称（便于识别）
3. 粘贴 API Key
4. 状态选择「启用」
5. 点击「保存」

**Key 状态管理：**
- ✅ 启用：正常参与负载均衡
- ⏸️ 禁用：暂时不使用，保留配置
- 🗑️ 删除：永久移除

**统计信息：**
- 成功次数：该 Key 成功请求的数量
- 失败次数：该 Key 请求失败的数量
- 最后使用：最近一次调用时间

#### 📋 各平台「获取模型」
每个平台卡片右上角有独立按钮，弹窗互不混用：

| 平台 | 按钮 | 管理 API（需 `X-Bridge-Token: ADMIN_TOKEN`） |
|------|------|---------------------------------------------|
| Google | Gemini 模型 | `GET /admin/api/models/google`（旧路径 `GET /admin/api/models` 等同） |
| OpenAI | OpenAI 模型 | `GET /admin/api/models/openai` |
| Anthropic | Anthropic 模型 | `GET /admin/api/models/anthropic` |

#### 📊 使用统计
- 每个平台独立统计总调用次数
- 自动轮询分配请求到可用的 Key

---

## API 调用指南

### 鉴权方式
所有代理请求都需要在 Header 中携带鉴权信息：

**方式一（推荐）：**
```http
X-Bridge-Token: 你的BRIDGE_TOKEN
```

**方式二（OpenAI 兼容）：**
```http
Authorization: Bearer 你的BRIDGE_TOKEN
```

---

### 路由与协议约定

| 类型 | 路径示例 | 上游实际协议 |
|------|-----------|--------------|
| Gemini 原生 | `POST /google/v1beta/models/{model}:generateContent` 等 | Google Generative Language（Gemini） |
| Gemini → 下游 OpenAI 形态 | `POST /google/v1/chat/completions` | 网关转 Gemini 原生 + 响应转回 OpenAI JSON/SSE |
| Claude 原生 | `POST /anthropic/v1/messages` | Anthropic Messages API（`x-api-key`） |
| Claude → 下游 OpenAI 形态 | `POST /anthropic/v1/chat/completions` | Anthropic 官方 **OpenAI 兼容**端点（`Authorization: Bearer` + Claude Key） |
| 仅路径改写（与上表等价） | `POST /compat/openai/google/v1/chat/completions`<br>`POST /compat/openai/anthropic/v1/chat/completions` | 分别同「Gemini OpenAI 形态」「Claude OpenAI 形态」 |

**鉴权**：客户端 → 网关始终使用 **桥接令牌**（`X-Bridge-Token` 或 `Authorization: Bearer` 的值为 `BRIDGE_TOKEN`）。网关 → 上游自动注入对应平台的 API Key，**勿**把 OpenAI Key 当作桥接令牌传给网关（若误放在 `Authorization` 中会被剥除后由网关重写）。

**能力边界**：以各上游文档为准；Claude 的 OpenAI 兼容层对部分字段会静默忽略，且官方标注更适合评估/迁移场景，生产若需完整 Claude 能力请优先 **原生** `/anthropic/v1/messages`。

---

### Google Gemini API

#### 原生方式调用
```
POST https://你的域名/google/v1beta/models/gemini-1.5-flash:generateContent
```

**请求示例（curl）：**
```bash
curl -X POST "https://你的域名/google/v1beta/models/gemini-1.5-flash:generateContent" \
  -H "Content-Type: application/json" \
  -H "X-Bridge-Token: 你的BRIDGE_TOKEN" \
  -d '{"contents": [{"parts":[{"text": "你好"}]}]}'
```

#### OpenAI 兼容方式调用
```
POST https://你的域名/google/v1/chat/completions
```

**请求示例：**
```bash
curl -X POST "https://你的域名/google/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -H "X-Bridge-Token: 你的BRIDGE_TOKEN" \
  -d '{
    "model": "gemini-1.5-flash",
    "messages": [{"role": "user", "content": "你好"}],
    "stream": false
  }'
```

**流式响应示例：**
```bash
curl -N -X POST "https://你的域名/google/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -H "X-Bridge-Token: 你的BRIDGE_TOKEN" \
  -d '{
    "model": "gemini-1.5-flash",
    "messages": [{"role": "user", "content": "写一首诗"}],
    "stream": true
  }'
```

**compat 前缀（与上式等价）：** `POST https://你的域名/compat/openai/google/v1/chat/completions`，请求体与 Header 相同。

---

### OpenAI API

```
POST https://你的域名/openai/v1/chat/completions
```

**请求示例：**
```bash
curl -X POST "https://你的域名/openai/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -H "X-Bridge-Token: 你的BRIDGE_TOKEN" \
  -d '{
    "model": "gpt-3.5-turbo",
    "messages": [{"role": "user", "content": "你好"}]
  }'
```

支持的 OpenAI 接口：
- `/v1/chat/completions` - 聊天补全
- `/v1/completions` - 文本补全
- `/v1/embeddings` - 嵌入向量
- 以及其他所有 OpenAI 官方接口

---

### Anthropic (Claude) API

#### 原生方式（Messages API）

```
POST https://你的域名/anthropic/v1/messages
```

**请求示例：**

```bash
curl -X POST "https://你的域名/anthropic/v1/messages" \
  -H "Content-Type: application/json" \
  -H "X-Bridge-Token: 你的BRIDGE_TOKEN" \
  -d '{
    "model": "claude-sonnet-4-20250514",
    "max_tokens": 1024,
    "messages": [{"role": "user", "content": "你好"}]
  }'
```

#### OpenAI 形态（官方兼容层，非流式 / 流式）

下游使用与 OpenAI Chat Completions 相同的 JSON 字段习惯（如 `model`、`messages`、`stream`、`temperature`、`max_tokens` 等）；网关将请求转发至 `https://api.anthropic.com/v1/chat/completions`，**响应与 SSE 与 OpenAI 客户端预期一致**（由 Anthropic 兼容层生成）。

```
POST https://你的域名/anthropic/v1/chat/completions
```

**非流式示例：**

```bash
curl -X POST "https://你的域名/anthropic/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -H "X-Bridge-Token: 你的BRIDGE_TOKEN" \
  -d '{
    "model": "claude-sonnet-4-20250514",
    "messages": [{"role": "user", "content": "用一句话介绍你自己"}],
    "stream": false
  }'
```

**流式示例（SSE）：**

```bash
curl -N -X POST "https://你的域名/anthropic/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -H "X-Bridge-Token: 你的BRIDGE_TOKEN" \
  -d '{
    "model": "claude-sonnet-4-20250514",
    "messages": [{"role": "user", "content": "写一首四句诗"}],
    "stream": true
  }'
```

**与 compat 前缀等价示例：**

```bash
curl -X POST "https://你的域名/compat/openai/anthropic/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -H "X-Bridge-Token: 你的BRIDGE_TOKEN" \
  -d '{"model":"claude-sonnet-4-20250514","messages":[{"role":"user","content":"hi"}]}'
```

**说明**：兼容层能力与限制见 [Anthropic — OpenAI SDK compatibility](https://docs.anthropic.com/en/api/openai-sdk)。未实现的 OpenAI 路径（如 **`/v1/responses`**）请勿通过本网关期待跨厂商一致行为。

### 与原接口对照

| 原接口（直连厂商） | 经 LiuAIbridge |
|-------------------|----------------|
| `https://generativelanguage.googleapis.com/v1beta/...` | `https://你的域名/google/v1beta/...` |
| `https://api.openai.com/v1/...` | `https://你的域名/openai/v1/...` |
| `https://api.anthropic.com/v1/messages`（原生） | `https://你的域名/anthropic/v1/messages` |
| `https://api.anthropic.com/v1/chat/completions`（Anthropic 提供的 OpenAI 兼容） | `https://你的域名/anthropic/v1/chat/completions` 或 `.../compat/openai/anthropic/v1/chat/completions` |
| （无，网关内转换）Gemini + OpenAI 形态 | `https://你的域名/google/v1/chat/completions` 或 `.../compat/openai/google/v1/chat/completions` |

## 常见问题

### ❓ Q: 部署后调用返回 401 "Invalid Bridge Token"
**A:** 检查以下几点：
1. 确认已正确设置 `BRIDGE_TOKEN` secret
2. 检查请求 Header 中 `X-Bridge-Token` 的值是否正确
3. Token 前后不要有多余的空格或换行符

### ❓ Q: 调用返回 "Service not configured"
**A:** 需要先在管理后台配置对应平台的 API Key：
1. 访问 `/admin` 页面
2. 添加并启用对应平台的 API Key
3. 保存后重试

### ❓ Q: 本地调试可以正常调用，部署后不行
**A:** 这是正常现象！本地运行时，请求从你的电脑发出，需要你自己的网络能够访问目标 API。部署到 Cloudflare 后，请求从 Cloudflare 节点发出，国内可直接访问。

### ❓ Q: 如何更新 API Key？
**A:** 
1. 登录 `/admin` 后台
2. 找到对应平台的 Key 列表
3. 编辑目标 Key 或添加新 Key
4. 点击「保存」立即生效

### ❓ Q: 支持多少个 API Key？
**A:** 理论上没有限制，可以添加多个 Key，系统会自动轮询使用，实现负载均衡和故障转移。

### ❓ Q: Token 泄露了怎么办？
**A:** 立即重新设置 Token：
```bash
wrangler secret put ADMIN_TOKEN
wrangler secret put BRIDGE_TOKEN
```
然后重新部署即可生效。

### ❓ Q: 是否支持流式响应？
**A:** 支持。**OpenAI 直连**、**Claude OpenAI 形态**（`/anthropic/v1/chat/completions`）为上游 SSE **透传**；**Gemini OpenAI 形态**由网关将 Google 流式结果转为 OpenAI 风格 SSE。使用 `stream: true` 时请使用支持 SSE 的客户端（如 `curl -N`）。

### ❓ Q: 如何查看调用日志？
**A:** 登录 Cloudflare Dashboard → Workers & Pages → 你的 Worker → 「日志」标签页，可以查看实时请求日志。

---

## 技术支持

- GitHub 仓库：[https://github.com/lekliu/LiuAIbridge](https://github.com/lekliu/LiuAIbridge)
- Issues：如有问题请在 GitHub 提交 Issue

---

## 许可证

本项目采用 MIT 许可证开源。
